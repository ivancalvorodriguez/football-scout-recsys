outputs/
open-data/
.env/
# Configuracion del despliegue con Docker: ahi acaba la clave de firma de la
# sesion. El fichero versionado es `.env.example`.
.env
.claude/
.dev/

__pycache__/

# Cobertura (pytest --cov).
.coverage

# Pruebas de mutacion (mutmut): arbol de mutantes y cache, regenerables.
mutants/
.mutmut-cache
html/
